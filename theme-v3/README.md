# Theme-v3-Spiegel (OFE v3 — Experience)

Shopify-Theme `gid://shopify/OnlineStoreTheme/194124087629` (UNPUBLISHED).

- `original/` — Stand vor den Website-Design-Updates (Rollback-Basis).
  Liquid/JS/CSS byte-identisch zum Remote-Stand (MD5-verifiziert).
  JSON-Templates semantisch identisch, aber neu serialisiert (Formatierung
  weicht ab — Shopify normalisiert beim Upsert ohnehin).
- `current/` — Arbeitsstand, wird per themeFilesUpsert ins Theme gespiegelt.
