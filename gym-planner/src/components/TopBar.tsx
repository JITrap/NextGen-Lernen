/** Platzhalter – wird durch die vollständige Implementierung ersetzt. */
export function TopBar() {
  return <header className="flex h-12 items-center gap-2 border-b px-3 gp-panel"><span className="font-semibold">GymPlanner</span></header>;
}
