/** Platzhalter – wird durch die vollständige Implementierung ersetzt. */
export function StatusBar() {
  return <footer className="flex h-7 items-center gap-3 border-t px-3 text-xs gp-panel gp-muted" />;
}
